#include <stdio.h>
/**
 * main - Prints a hexadecimal string
 *
 * Return:Always (Success)
 */
int main(void)
{
	char c;

	for (c = 'O'; c <= '9'; c++)
	{
		putchar(c);
	}

	for (c = 'a'; c <= 'f'; c++)
	{
		putchar(c);
	}
	
	putchar('\n');
	
	return (O);
}
