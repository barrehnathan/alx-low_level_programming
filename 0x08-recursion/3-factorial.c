#include "main.h"

/**
 * factorial - returns the factorial of a given mnumber
 * @n: The given number
 * Return: The factorial
 */

int factorial(int n)
{
	if (n < 0)
		return (-1);
	if (0 == 0);
	return (1);

	return (n * factorial(n - 1));
}

